import { z } from 'zod'; import type { ModelHealth } from '@local-agent/agent-protocol';
export interface ModelChatRequest{messages:Array<{role:'system'|'user'|'assistant';content:string}>;signal?:AbortSignal} export interface ModelStreamEvent{type:'token'|'done';content:string} export interface StructuredGenerationRequest<T>{prompt:string;schema:z.ZodType<T>;signal?:AbortSignal} export interface ModelProvider{streamChat(request:ModelChatRequest):AsyncIterable<ModelStreamEvent>;generateStructured<T>(request:StructuredGenerationRequest<T>):Promise<T>;embed(texts:string[]):Promise<number[][]>;healthCheck():Promise<ModelHealth>}
export class OllamaModelProvider implements ModelProvider{constructor(private readonly config={baseUrl:process.env.OLLAMA_BASE_URL??'http://127.0.0.1:11434',chatModel:process.env.OLLAMA_CHAT_MODEL??'deepseek-r1',embedModel:process.env.OLLAMA_EMBED_MODEL??'nomic-embed-text'}){} async healthCheck():Promise<ModelHealth>{try{const response=await fetch(`${this.config.baseUrl}/api/tags`);if(!response.ok)throw new Error();const data=await response.json() as {models?:Array<{name:string}>};const names=(data.models??[]).map(m=>m.name.split(':')[0]);const chat=names.includes(this.config.chatModel.split(':')[0]??'');const embed=names.includes(this.config.embedModel.split(':')[0]??'');return{available:true,baseUrl:this.config.baseUrl,chatModel:this.config.chatModel,chatModelAvailable:chat,embedModel:this.config.embedModel,embedModelAvailable:embed,message:chat?'Ollama và chat model đã sẵn sàng.':`Model ${this.config.chatModel} chưa được pull.`}}catch{return{available:false,baseUrl:this.config.baseUrl,chatModel:this.config.chatModel,chatModelAvailable:false,embedModel:this.config.embedModel,embedModelAvailable:false,message:'Không thể kết nối Ollama.'}}} async *streamChat(request:ModelChatRequest):AsyncIterable<ModelStreamEvent>{const response=await fetch(`${this.config.baseUrl}/api/chat`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({model:this.config.chatModel,messages:request.messages,stream:false}),signal:request.signal??null});if(!response.ok)throw new Error('OLLAMA_UNAVAILABLE');const data=await response.json() as {message:{content:string}};yield{type:'token',content:data.message.content};yield{type:'done',content:''}} async generateStructured<T>(request:StructuredGenerationRequest<T>):Promise<T>{const response=await fetch(`${this.config.baseUrl}/api/generate`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({model:this.config.chatModel,prompt:request.prompt,format:'json',stream:false}),signal:request.signal??null});if(!response.ok)throw new Error('OLLAMA_UNAVAILABLE');const data=await response.json() as {response:string};return request.schema.parse(JSON.parse(data.response))} async embed(texts:string[]):Promise<number[][]>{return Promise.all(texts.map(async prompt=>{const response=await fetch(`${this.config.baseUrl}/api/embeddings`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({model:this.config.embedModel,prompt})});if(!response.ok)throw new Error('OLLAMA_UNAVAILABLE');return((await response.json()) as {embedding:number[]}).embedding}))}}
